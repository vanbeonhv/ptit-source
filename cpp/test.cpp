#include <bits/stdc++.h>

using namespace std;

int main() {
#ifndef ONLINE_JUDGE
  freopen("input.txt", "r", stdin);
  freopen("output.txt", "w", stdout);
#endif
  cout << "tset";
  // int n, target, a[100];
  // cin >> n;
  // unordered_map<int, int> mp;
  // for (int i = 0; i < n; i++) {
  //   cin >> a[i];
  //   mp[a[i]] = i;
  // }

  // cin >> target;

  // for (int i = 0; i < n; i++) {
  //   int x = target - a[i];
  //   if (mp[x] > 0) {
  //     cout << i << " " << mp[x];
  //     break;
  //   }
  // }
}